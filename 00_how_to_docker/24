# doesn't run through daemon have to run:
#   docker-machine ssh Char
docker service create --name orbital-command --hostname orbital-command --env RABBITMQ_DEFAULT_USER=docker --env RABBITMQ_DEFAULT_PASS=isdope --network overmind rabbitmq
